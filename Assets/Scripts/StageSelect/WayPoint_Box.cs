using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace TeamProject
{

    public class WayPoint_Box : MonoBehaviour
    {
        public float[] Stage_WayPoint;//ステージの正面に位置するドリーのパスの位置を入れる用
        public float[] Exit_WayPoint;//ステージから出口までのドリーのパスの位置を入れる用
        public float[] World_WayPoint;//ワールド間のドリーのパスの位置を入れる用
        // Start is called before the first frame update
        //void Start()
        //{

        //}

        //// Update is called once per frame
        //void Update()
        //{

        //}
    }//public class WayPoint_Box : MonoBehaviour END
}//namespace END